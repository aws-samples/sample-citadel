// Empty CSS module stub for jest. Real CSS is bundled by Vite at build time;
// the jest test runner does not need (and cannot parse) CSS source. The
// jest.config.cjs `moduleNameMapper` redirects every `*.css` import to this
// file so any `import 'foo.css'` resolves to an empty object in tests.
module.exports = {};
